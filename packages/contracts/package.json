{
  "name": "@romeo/contracts",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "files": [
    "src/**/*.ts",
    "!src/**/*.test.ts"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.4.0",
    "@romeo/auth": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
