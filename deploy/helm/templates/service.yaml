apiVersion: v1
kind: Service
metadata:
  name: {{ include "romeo.fullname" . }}
  labels:
    {{- include "romeo.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "romeo.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
